# 实验室样本交接看板（Labs Handover Dashboard）

本地实验室样本交接追溯系统，支持样本登记、批量交接、退回重交、签收、温控异常告警、角色权限、审计历史和筛选导出。

## ✨ 核心功能

| 功能模块 | 说明 |
|---|---|
| 🔐 角色权限 | **采样员（SAMPLER）**登记/发起交接，**接收员（RECEIVER）**签收/退回，**管理员（ADMIN）**仅可作废、审计、全量查看 |
| 📦 样本登记 | 批量登记，自动创建批次，**初始温度必填**，缺失则拒绝 |
| 🚀 发起交接 | 按批次指定接收人发起交接；交接前校验温控完整性 |
| ✅ 签收 | 只有**指定接收人**可签收；**严禁替签**；签收时每个样本需提供温度记录；**已签收后不可退回、不可改交接信息** |
| ↩️ 退回重交 | 仅**IN_TRANSIT（交接中）**状态可退回；退回**必须填写原因**，缺原因直接拒绝；**已签收样本不可退回**；退回后样本返还给采样员，可重新发起交接 |
| 🌡️ 温控异常 | 每次记录温度自动检测阈值（过高/过低），生成异常告警；支持标记处理 |
| 🔍 完整追溯 | 每个样本可查**当前保管人**、**状态**、**全部温控记录**、**完整交接链时间线**（登记→交接→签收→退回→重交→作废） |
| 🛡️ 审计日志 | 记录所有操作的操作者、时间、目标、前后状态、失败原因；**失败操作也记录** |
| 📊 筛选导出 | 按批次状态/样本状态/关键字/温控异常过滤；CSV 导出内容**严格匹配筛选条件**，包含批次/样本/温控/交接链四张表 |
| 📥 CSV 批量导入 | 从 CSV 一次导入多个样本和温控记录；**导入前全量校验**（字段格式、重复样本号、接收人权限）；发现冲突**不半写入**，返回可定位的错误行和原因 |
| 📋 预收样模板 | 保存常用模板（接收人、保存条件、运输要求、默认温控阈值、备注），导入 CSV 时直接套用；**模板快照机制**，修改模板不影响已创建批次 |
| 📝 导入草稿 | 未提交的导入任务保存为草稿，换人登录或后端重启后可继续编辑；**乐观锁并发控制**，同一份草稿多人编辑时明确提示冲突；支持**取消草稿**（保留记录），取消后不可再修改 |
| ↩️ 撤销导入 | 撤销最近一次成功导入，仅发起人或管理员可操作；完整回滚批次、样本、温控告警、交接记录、模板引用、关联草稿和导出结果；关联草稿自动回退为可编辑状态 |
| ⚙️ 可复用导出配置 | 保存导出配置（是否带签收历史/温控异常/失败审计），筛选条件与页面结果一致；下次按配置一键导出 |
| 📋 操作日志 | CSV 导入、冲突拦截、模板变更、草稿恢复、冲突拦截、撤销人、导出配置增删改、实际导出人均写入审计日志 |
| 💾 数据持久化 | JSON 文件存储（`data/db.json`），重启服务历史数据与退回再交接时间线**完全保留** |
| ⚠️ 原子操作 | 所有操作先校验再执行，**失败不写入半截记录** |

---

## 🔑 样例账号（开发环境预置）

| 角色 | 用户名 | 密码 | 姓名 | 部门 |
|---|---|---|---|---|
| 采样员 | `sampler1` | `sampler123` | 张采样 | 采样部 |
| 采样员 | `sampler2` | `sampler123` | 李采样 | 采样部 |
| 接收员 | `receiver1` | `receiver123` | 王接收 | 检验科 |
| 接收员 | `receiver2` | `receiver123` | 赵接收 | 检验科 |
| 管理员 | `admin1` | `admin123` | 孙管理 | 质量管理部 |

登录页点击账号卡片可一键填入。

---

## 🚀 快速开始

```bash
# 1. 安装依赖
npm install

# 2. 启动（前端 + 后端同时启动）
npm run dev

# 或分别启动
npm run server:dev    # 后端：http://localhost:3001
npm run client:dev    # 前端：http://localhost:5173
```

默认打开 `http://localhost:5173` 登录即可。

> 💡 数据存储在 `./data/db.json`，首次启动自动创建并初始化账号。要完全重置，删除该文件后重启服务。

---

## 🧪 功能验证步骤（完整测试清单）

### 1️⃣ 场景一：正常流程：登记 → 发起交接 → 签收

1. 登录 **sampler1 / sampler123**（采样员）
2. 左侧菜单 **样本登记**，添加 2 条样本：
   - 样本编号 `S-TEST-001` / `S-TEST-002`，初始温度 4℃（正常范围 2-8℃）
   - 点击【登记并创建批次】
3. 跳转进入批次详情页 → 点击【发起交接】→ 指定接收人为 **王接收（receiver1）** → 确认
4. **登出**，登录 **receiver1 / receiver123**（接收员）
5. 进入批次详情，点击【签收】→ 为两个样本填写签收温度（如 4.2℃ / 3.8℃）→ 确认
6. ✅ 验证点：
   - 批次状态变为 **全部签收**；样本状态变为 **已签收**；保管人变为「王接收」
   - 每个样本的**交接链**按时间线展示：登记 → 发起交接 → 签收
   - **审计历史**（管理员查看）有 3 条成功记录
   - 签收时的温度记录已入库

---

### 2️⃣ 场景二：退回重交（含时间线持久化验证）

> ⚠️ **硬约束**：仅 IN_TRANSIT（交接中）状态的样本可以退回，已签收样本**不可退回**。

1. 登录 **sampler1**，新建一批次并发起交接给 **王接收（receiver1）**（样本状态 = IN_TRANSIT）
2. 登录 **receiver1**，进入批次详情
3. 点击【退回】→ 不填原因直接点确认 → ✅ **应该报错「退回必须填写原因」**，操作失败
4. 填写原因「冷链温度异常怀疑」，为每个样本填温度（如 12℃，故意高于阈值）→ 确认
5. ✅ 验证点：
   - 样本状态变为「已退回」；保管人回到「张采样」
   - 交接链新增**退回**节点，附带退回原因
   - 12℃ 自动生成「温度过高」温控异常告警
6. **重启后端服务**（`Ctrl+C` 再 `npm run server:dev`），刷新页面
7. 重新登录 **sampler1**，进入同批次 → ✅ **退回记录和完整时间线依然存在**
8. 点击【重新交接】→ 指定接收人 **赵接收（receiver2）**，为退回样本填写新温度 5℃ → 确认
9. ✅ 验证点：交接链新增「重新交接」节点，重启数据不丢失

---

### 3️⃣ 场景三：防止替人签收（关键校验）

1. 登录 **sampler1**，新建一批次，指定接收人为 **王接收（receiver1）**
2. 登出，登录 **receiver2 / receiver123**（赵接收，非指定人）
3. 进入批次详情 → ✅ **签收按钮置灰**，并有红色提示：「非指定接收人，禁止替人签收」
4. 强制尝试（用工具调用 API）→ ✅ 后端返回 400 错误并写入**失败审计**（带失败原因），**不写入半截记录**

---

### 4️⃣ 场景四：温控缺失时拒绝发起交接

1. 用任意方式手动清除某个样本的 `temperatureRecords`（或调用 API 模拟）
2. 在批次详情点击【发起交接】→ ✅ **报错：样本缺少温控记录，无法发起交接**
3. ✅ 批次状态未改变；没有半截的交接记录；审计中有失败记录

---

### 5️⃣ 场景五：已签收后不可退回、不可改交接信息

1. 把一批次按场景一完成签收（样本状态 = RECEIVED）
2. 尝试调用 `returnBatch` 或前端退回按钮 → ✅ **报错：已签收样本不可退回**
3. 尝试调用 `initiateHandover` 或前端再点发起交接 → ✅ **报错：样本当前状态不允许发起交接**
4. ✅ 验证点：
   - 已签收记录保持不变，交接链没有被追加错误节点
   - 温控记录未被篡改
   - 审计中有失败记录（含明确的失败原因）
   - **无半截记录**——拒绝操作时 db.json 无任何变化

---

### 6️⃣ 场景六：管理员作废（不删除，仅标记）

1. 登录 **admin1 / admin123**（管理员）
2. 进入任意批次详情 → 点击【作废】→ 填写原因「业务取消」→ 确认
3. ✅ 验证点：
   - 样本状态标记为「已作废」，但历史数据完整
   - 交接链末尾追加「作废」节点
   - **没有删除**任何记录，完整可追溯
   - 其他角色无法看到【作废】按钮

---

### 7️⃣ 场景七：筛选 + 导出一致性

1. 登录任意角色 → 进入 **批次查询**
2. 设置筛选条件：状态 = 交接中，关键字 = 某个批次号
3. 点击【导出 CSV】→ 打开下载文件
4. ✅ 验证点：
   - 文件头部注释中记录了与筛选条件一致的 JSON
   - 批次列表、样本明细、温控记录、交接链历史均只包含筛选出的范围
   - 温控异常的样本在温控记录表中被标记为「是」
   - 签收历史在交接链表中完整出现

---

### 8️⃣ 场景八：CSV 批量导入（成功 + 冲突 + 无权限）

1. 准备 CSV 文件 `test.csv`，内容如下：
```csv
sampleCode,type,description,sourceLocation,minTemp,maxTemp,initialTemperature,tempLocation
IMP-001,血液,导入测试1,A栋采样室,2,8,4.5,A栋冷藏柜
IMP-002,尿液,导入测试2,B栋采样室,2,8,3.8,B栋冷藏柜
```
2. 登录 **sampler1** → 进入 **批次查询** → 点击【CSV 导入】→ 选择文件
3. ✅ 验证点：
   - 导入成功，提示"成功导入 2 个样本"
   - 批次列表、交接详情、温控异常中可看到新增数据
   - 每个导入样本有初始温度记录和交接链
4. 再次导入同一文件 → ✅ **报错：样本编号 IMP-001 已存在**，返回错误行列表
5. 登录 **receiver1** → 尝试 CSV 导入 → ✅ **403 无权限**

#### 自动化测试

```bash
# 确保服务已启动
node test-import-export.mjs
```

该脚本覆盖：成功导入、重复冲突、无权限导入、字段校验、接收人权限检查、导出配置 CRUD、按配置导出、操作日志验证。

---

### 9️⃣ 场景九：导出配置 + 按配置导出

1. 登录任意角色 → 进入 **批次查询** → 点击齿轮⚙️按钮
2. 勾选「包含失败审计」，输入配置名称「含审计导出」→ 点击保存
3. 从配置下拉选择「含审计导出」→ 点击【导出 CSV】
4. ✅ 验证点：
   - 导出文件包含「失败审计记录」表
   - 文件头部注释包含「导出配置: 含审计导出」
   - 筛选条件与页面结果一致
5. 删除该配置 → ✅ 下拉列表中不再出现

---

### 🔟 场景十：重启后校验（导入数据持久化）

1. 按「场景八」导入样本
2. **重启后端服务**（Ctrl+C → `npm run server:dev`）
3. 刷新页面 → ✅ 验证点：
   - 导入的批次仍在列表中，状态为「草稿」
   - 样本的交接链完整（含系统→采样员的登记节点）
   - 温控记录和异常告警未丢失
   - 审计日志中有之前的 CSV_IMPORT 记录

---

### 1️⃣1️⃣ 场景十一：预收样模板管理（快照隔离）

1. 登录 **sampler1** → 进入 **批次查询** → 点击【模板管理】
2. 创建模板：
   - 名称：`常规血液模板`
   - 接收人：**王接收（receiver1）**
   - 保存条件：`2-8℃冷藏，避免冷冻`
   - 运输要求：`冷链运输，全程温度监控`
   - 默认温控：最小 `2℃`，最大 `8℃`
   - 备注：`日常血液采样标准配置`
3. ✅ 验证点：
   - 模板创建成功，版本号 = 1，引用计数 = 0
   - 模板列表中可见

4. 编辑模板，修改默认温度为 `1-8℃`
   - ✅ 验证点：模板版本号 = 2

5. 准备 CSV 文件：
```csv
sampleCode,type,description,sourceLocation,minTemp,maxTemp,initialTemperature,tempLocation
TPL-TEST-001,blood,template test 1,Building A,5,10,4.5,Freezer A
TPL-TEST-002,blood,template test 2,Building B,5,10,3.8,Freezer B
```
6. 点击【CSV 导入】→ 选择文件 → 选择模板 `常规血液模板` → 确认导入
7. ✅ 验证点：
   - 导入成功，批次自动关联模板快照
   - 批次 `templateSnapshot.templateVersion = 2`（保存时的版本）
   - 样本温度范围被模板覆盖为 `1-8℃`（不是 CSV 中的 5-10）
   - 模板引用计数 = 1

8. 再次修改模板，修改默认温度为 `0-10℃`，版本号变为 3
9. ✅ 验证点：
   - 已创建批次的 `templateSnapshot.templateVersion` 仍为 2
   - 批次样本温度范围仍为 `1-8℃`（不受模板修改影响）
   - 模板快照完整保留了创建时的所有字段

---

### 1️⃣2️⃣ 场景十二：导入草稿保存与跨重启恢复

1. 登录 **sampler1** → 进入 **批次查询** → 点击【CSV 导入】
2. 选择 CSV 文件（3-5 行数据）→ 选择模板 → 点击【保存草稿】
3. 输入草稿名称：`待确认的门诊样本` → 确认保存
4. ✅ 验证点：
   - 草稿保存成功，版本号 = 1
   - 草稿列表中可见，状态为 `DRAFT`
   - 草稿包含模板快照

5. **登出** sampler1，登录 **sampler2**
   - ✅ 验证点：sampler2 无法看到 sampler1 的草稿（权限隔离）

6. 登出 sampler2，重新登录 **sampler1**
   - ✅ 验证点：草稿仍在列表中，内容完整

7. **重启后端服务**（`Ctrl+C` → `npm run server:dev`）
8. 刷新页面，重新登录 sampler1
   - ✅ 验证点：
     - 草稿仍在列表中，内容未丢失
     - 草稿版本号、CSV 内容、模板快照完整保留
     - 草稿可以继续编辑或提交导入

9. 打开草稿 → 修改 CSV 内容 → 点击【保存】
   - ✅ 验证点：草稿版本号 = 2，最后编辑者 = `张采样`

---

### 1️⃣3️⃣ 场景十三：并发草稿冲突检测

1. 登录 **sampler1**，创建一份草稿（版本 1），不关闭页面
2. 打开另一个浏览器/隐身窗口，登录 **admin1**（管理员可见所有草稿）
3. 管理员打开同一份草稿，修改内容并保存
   - ✅ 验证点：草稿版本号 = 2，最后编辑者 = `孙管理`

4. 回到 sampler1 的页面，尝试保存修改（客户端版本 = 1）
   - ✅ 验证点：
     - 返回 409 冲突错误
     - 提示：`草稿已被他人修改，请刷新后重新编辑`
     - 返回冲突信息：当前版本 2，最后编辑者 `孙管理`
     - 草稿内容未被 sampler1 的旧版本覆盖
     - 审计日志记录 `DRAFT_CONFLICT_BLOCKED`

5. sampler1 刷新页面获取最新版本（版本 2），重新编辑后保存
   - ✅ 验证点：保存成功，版本号 = 3

6. 尝试提交导入时也使用旧版本号
   - ✅ 验证点：同样触发冲突拦截，防止提交过期数据

---

### 1️⃣3️⃣.5 场景十三.5：取消草稿

1. 登录 **sampler1**，创建一份草稿
2. 点击【取消草稿】
   - ✅ 验证点：
     - 草稿状态变为 `CANCELLED`
     - 草稿仍可见但不可再修改或提交
     - 审计日志记录 `CANCEL_DRAFT`

3. 尝试修改已取消的草稿
   - ✅ 验证点：报错「草稿已提交或已取消，无法修改」

4. 尝试取消已取消的草稿
   - ✅ 验证点：报错「只有草稿状态的导入任务可以取消」

5. 登录 **receiver1** 尝试取消 sampler1 的草稿
   - ✅ 验证点：403 权限不足

6. 区别于「删除草稿」：取消保留记录（可追溯），删除则彻底移除

---

### 1️⃣3️⃣.6 场景十三.6：撤销导入后草稿自动回退

1. 登录 **sampler1**，创建一份草稿并提交导入（草稿状态 = IMPORTED）
2. 撤销该次导入
   - ✅ 验证点：
     - 批次和样本被删除
     - 关联草稿状态自动从 `IMPORTED` 回退为 `DRAFT`
     - 草稿的 `batchId` 被清除
     - 撤销结果中 `draftReverted = true`
     - 审计日志记录 `UNDO_REVERT_DRAFT`
     - 草稿可重新编辑或再次提交

3. 如果撤销的导入并非来自草稿（直接 CSV 导入）
   - ✅ 验证点：撤销结果中 `draftReverted = false`，无草稿回退

---

### 1️⃣4️⃣ 场景十四：撤销最近一次成功导入（权限控制）

1. 登录 **sampler1**，导入一批样本（2-3 个）
2. ✅ 验证点：
   - 导入成功，批次、样本、温控记录、交接链完整
   - 审计日志记录 `CSV_IMPORT`
   - 生成撤销记录（未撤销状态）

3. **登出** sampler1，登录 **sampler2**
4. 调用撤销 API（或前端撤销按钮）
   - ✅ 验证点：
     - 撤销失败，报错：`只能撤销自己创建的导入记录`
     - 审计日志记录 `UNAUTHORIZED_UNDO_ATTEMPT`
     - 批次和样本完好无损

5. 登出 sampler2，登录 **receiver1**（接收员）
6. 尝试撤销
   - ✅ 验证点：撤销失败，提示无权限或无可撤销记录

7. 登录 **admin1**（管理员）
8. 调用撤销
   - ✅ 验证点：管理员可以撤销任何人的导入记录

---

### 1️⃣5️⃣ 场景十五：撤销导入 - 完整回滚验证

1. 登录 **sampler1**，导入一批样本（2 个）：
```csv
sampleCode,type,description,sourceLocation,minTemp,maxTemp,initialTemperature,tempLocation
UNDO-TEST-001,blood,undo test 1,Building A,2,8,4.0,Freezer A
UNDO-TEST-002,blood,undo test 2,Building B,2,8,3.5,Freezer B
```
2. ✅ 导入前记录：
   - 批次列表中有新批次
   - 样本列表中有 2 个新样本
   - 每个样本有温控记录和交接链
   - 导出 CSV 中包含这些样本

3. 点击【撤销最近一次导入】→ 确认撤销
4. ✅ 验证点（回滚完整性）：
   - 批次从批次列表中消失（完全删除）
   - 2 个样本从样本列表中消失
   - 所有相关温控记录被清除
   - 所有相关交接链被清除
   - 所有相关温控告警被清除
   - 导出 CSV 中不再包含这些样本
   - 模板引用计数递减（如有使用模板）
   - 关联草稿回退为 DRAFT 状态（如导入来自草稿）
   - 关联导出配置中引用已删除批次的 filter 被标记清理
   - 审计日志记录 `UNDO_IMPORT`，包含撤销前状态
   - 审计日志记录 `UNDO_REVERT_DRAFT`（如有关联回退）
   - 审计日志记录 `UNDO_CLEANUP_EXPORT_CONFIGS`（如有配置清理）
   - 撤销人 = `张采样`

5. 导出 CSV 验证：
   - 导出批次列表，确认撤销的批次不在其中
   - 导出样本列表，确认撤销的样本不在其中
   - 导出的温控记录表、交接链表也不包含已撤销数据

6. 查看撤销记录列表
   - ✅ 验证点：记录标记为 `undone = true`，包含撤销人、撤销时间

---

### 1️⃣6️⃣ 场景十六：撤销阻断 - 已流转样本不可撤销

1. 登录 **sampler1**，导入 1 个样本
2. 对该批次发起交接（样本状态变为 `IN_TRANSIT`）
3. 尝试撤销导入
   - ✅ 验证点：
     - 撤销失败，报错：`以下样本已开始流转，无法撤销：xxx`
     - 审计日志记录 `UNDO_BLOCKED`
     - 批次和样本保持完好
     - 撤销记录仍为未撤销状态

---

### 1️⃣7️⃣ 场景十七：模板被引用后的停用

1. 按场景十一创建模板并引用（引用计数 = 1）
2. 点击【停用模板】
   - ✅ 验证点：
     - 模板 `isActive = false`
     - 模板版本号递增
     - 模板从"仅显示启用"列表中消失
     - 在"显示全部"列表中仍可见

3. 尝试用已停用的模板导入 CSV
   - ✅ 验证点：
     - 导入失败，报错：`所选模板已停用`
     - 新批次无法创建

4. ✅ 验证旧批次不受影响：
   - 已创建的批次仍保留模板快照
   - 批次的 `templateSnapshot` 完整可用
   - 样本温度范围正确
   - 模板引用计数仍为 1（停用不影响计数）

5. 撤销使用该模板的导入
   - ✅ 验证点：模板引用计数递减为 0

---

### 🔬 自动化测试脚本

#### 全模块集成测试（test-full-module.mjs）

覆盖模板仓库 + 草稿恢复 + 撤销导入三大模块的完整链路测试：

**Part 1 - 模板仓库**：
- 模板创建/查询/更新
- 权限校验（接收员不可创建、非创建者不可修改/停用）
- 用模板导入 CSV（快照自动生成）
- 模板快照隔离（修改模板不影响已创建批次）
- 停用模板后旧批次快照保留

**Part 2 - 草稿恢复**：
- 草稿创建/查询/更新
- 草稿权限校验（非创建者不可修改）
- 并发冲突检测（409 + 冲突信息 + 审计日志）
- 取消草稿（保留记录，不可再修改）
- 删除草稿（权限隔离）
- 从草稿提交导入

**Part 3 - 撤销导入**：
- 撤销权限校验
- 完整回滚（批次、样本、模板引用、导出结果）
- 关联草稿自动回退
- 已流转样本禁止撤销
- 撤销记录查询权限

**Part 4 - 审计日志**：
- 所有新增操作的审计记录完整性验证

```bash
# 确保服务已启动（npm run dev 或 npm run server:dev）
node test-full-module.mjs
```

#### 模板/草稿/撤销功能测试（test-template-draft-undo.mjs）

覆盖 14 个测试场景，包含：
- 模板 CRUD + 权限校验
- 模板快照隔离（修改不影响旧批次）
- 草稿保存/恢复 + 权限隔离
- 并发冲突检测（保存时 + 提交时）
- 撤销导入权限控制（越权阻断）
- 撤销导入完整回滚（批次、样本、告警、交接链）
- 撤销阻断（已流转样本）
- 模板停用 + 引用计数
- 草稿提交导入
- 撤销后导出校验
- 草稿删除权限
- 撤销记录列表权限

```bash
# 确保服务已启动（npm run dev 或 npm run server:dev）
node test-template-draft-undo.mjs
```

#### 跨重启持久化验证（test-restart-draft.mjs）

验证重启后所有新数据的持久化：
- 模板（含版本、引用计数、启用状态）
- 草稿（含版本、CSV 内容、模板快照、最后编辑者）
- 撤销记录（含撤销状态、撤销人、撤销时间）
- 批次模板快照
- 所有新增审计日志

```bash
# 1. 先运行完整功能测试创建测试数据
node test-template-draft-undo.mjs

# 2. 重启后端服务（Ctrl+C 后重新启动）
# npm run server:dev

# 3. 运行跨重启验证
node test-restart-draft.mjs
```

#### 完整验证流程（推荐）

```bash
# 步骤 1: 运行基础导入导出测试
node test-import-export.mjs

# 步骤 2: 运行全模块集成测试（模板仓库+草稿恢复+撤销导入）
node test-full-module.mjs

# 步骤 3: 运行原有模板/草稿/撤销功能测试
node test-template-draft-undo.mjs

# 步骤 4: 重启服务
# Ctrl+C 停止服务，然后运行:
npm run server:dev

# 步骤 5: 运行跨重启验证
node test-restart-draft.mjs

# 步骤 6: 运行重启后基础数据验证
node test-restart.mjs
```

---

## 📁 项目结构

```
zyx-00117/
├── api/                          # Express 后端
│   ├── app.ts                    # 应用入口 + Session
│   ├── db.ts                     # JSON 持久化（data/db.json）
│   ├── types.ts                  # 共享类型
│   ├── services.ts               # 核心业务（登记/交接/签收/退回/重交/作废/温控/CSV导入/导出配置）
│   ├── audit.ts                  # 审计写入与查询
│   ├── middleware.ts             # 认证 + 角色中间件
│   └── routes/
│       ├── auth.ts               # 登录/登出/会话
│       ├── labs.ts               # 样本/批次主路由（含 CSV 导入、导出配置 CRUD）
│       └── audit.ts              # 审计查询 + CSV 导出（含配置选项）
├── src/                          # React 前端
│   ├── App.tsx                   # 路由总入口
│   ├── api.ts                    # 后端 API 封装（含导入/导出配置接口）
│   ├── types.ts                  # 前端类型与标签/颜色映射
│   ├── store.ts                  # Zustand 全局状态（登录/Toast）
│   ├── components/MainLayout.tsx # 主框架 + 导航
│   └── pages/                    # 各页面
│       ├── Login.tsx             # 登录 + 样例账号快捷卡
│       ├── Dashboard.tsx         # 总览看板
│       ├── RegisterSamples.tsx   # 批量登记
│       ├── BatchList.tsx         # 批次查询 + 导出 + CSV导入 + 导出配置
│       ├── SampleList.tsx        # 样本追溯 + 导出 + 导出配置
│       ├── BatchDetail.tsx       # 批次详情 + 所有操作面板
│       ├── SampleDetail.tsx      # 样本完整时间线
│       ├── QuickAction.tsx       # 快捷操作列表
│       ├── AuditLog.tsx          # 审计历史
│       └── UserList.tsx          # 账号展示
├── data/db.json                  # 数据存储（自动生成）
├── test-import-export.mjs        # 导入/导出自动化测试脚本
├── test-template-draft-undo.mjs  # 模板/草稿/撤销完整功能测试脚本
├── test-full-module.mjs          # 模板仓库+草稿恢复+撤销导入 全链路集成测试
├── test-restart-draft.mjs        # 模板/草稿跨重启持久化验证脚本
└── README.md
```

---

## 🔒 安全与合规设计要点

1. **原子事务**：所有操作先做全部校验（角色、状态流、权限、温控），任一条件不满足则整体失败，**不写部分数据**。
2. **状态流转矩阵**：`REGISTERED → IN_TRANSIT → RECEIVED（终态，不可退回）或 RETURNED（可重交）→ VOIDED（终态）`。签收后**硬锁定**，不能退回、不能改交接信息。
3. **失败必审计**：替签、缺原因、缺温度、状态错误、草稿冲突、越权撤销都以 `success=false` 写入审计，附失败原因。
4. **无删除语义**：管理员仅能**作废**标记，历史数据永久保留；撤销导入仅删除刚创建且未流转的批次和样本。
5. **温控与交接强绑定**：登记、签收、退回、重新交接**均要求温度记录**；交接前会再次校验所有样本已有温控记录。
6. **重启一致性**：所有数据落盘 `data/db.json`，包含完整交接链数组、模板、草稿、撤销记录，重启后按时间戳排序展示。
7. **模板快照隔离**：模板每次修改版本号递增，引用时生成完整快照。旧批次始终保留创建时的快照，不受后续模板修改影响。
8. **乐观锁并发控制**：草稿采用版本号乐观锁，多人同时编辑时旧版本提交会被拦截，返回最后编辑者信息，避免互相覆盖。
9. **撤销权限控制**：仅导入发起人或管理员可撤销，已流转（非 REGISTERED/DRAFT 状态）的样本不可撤销，防止破坏业务链路。撤销时自动回退关联草稿状态和清理引用已删除批次的导出配置。
10. **完整操作审计**：模板创建/修改/停用、草稿创建/更新/删除/取消/提交/冲突、撤销导入/越权尝试/阻断/草稿回退/导出配置清理，所有操作均记录审计日志，包含操作人、前后状态、失败原因。
11. **权限隔离**：草稿按创建人隔离（管理员除外），撤销记录按创建人隔离（管理员除外），模板修改仅创建人或管理员可操作，草稿编辑仅创建者或管理员可操作。
12. **引用计数与停用保护**：模板维护引用计数，停用后不影响已引用批次，但禁止新导入使用，确保数据一致性。撤销时引用计数正确递减。

---

## 🔧 已修复的硬约束问题

### 修复1：已签收样本不可退回

**问题**：状态流矩阵中 `RECEIVED` 允许转移到 `RETURNED`，`returnBatch()` 也允许 `RECEIVED` 状态的样本退回。导致已签收的样本可以被退回，破坏了签收的终态语义。

**根因**：`STATUS_FLOW[RECEIVED]` 包含 `'RETURNED'`；`returnBatch()` 中 `sample.status !== 'IN_TRANSIT' && sample.status !== 'RECEIVED'` 条件允许 RECEIVED 退回。

**修复**（最小改动 3 处）：
1. `api/services.ts`：`STATUS_FLOW[RECEIVED]` 从 `['RETURNED', 'VOIDED']` 改为 `['VOIDED']`
2. `api/services.ts`：`returnBatch()` 中 `sample.status` 校验从 `!== 'IN_TRANSIT' && !== 'RECEIVED'` 改为 `!== 'IN_TRANSIT'`
3. `src/pages/BatchDetail.tsx` + `QuickAction.tsx`：前端退回按钮仅在 `batch.status === 'IN_TRANSIT'` 时显示；样本选择器中退回操作只允许 `IN_TRANSIT` 状态

**复现脚本**（修复前会成功退回已签收样本，修复后返回 400 错误）：
```bash
# 1. sampler1 登记样本并发起交接
# 2. receiver1 签收（状态 = RECEIVED）
# 3. receiver1 尝试退回 → 修复前：成功；修复后：400 "已签收样本不可退回"
curl -X POST http://localhost:3001/api/labs/batches/{batchId}/return \
  -H "Content-Type: application/json" -b cookie.txt \
  -d '{"sampleIds":["..."], "reason":"测试", "temperatureRecords":[...]}'
```

**验收标准**：已签收退回 → 400 错误；样本状态仍为 RECEIVED；交接链无新节点；审计有失败记录。

### 修复2：CSV 导出连接被关闭

**问题**：`/export/batches` 和 `/export/samples` 路由使用 `res.write('\uFEFF')` + `res.send(csvContent)` 组合，Express 在 `write()` 后调用 `send()` 会重新设置 Content-Length 为仅 csvContent 的长度（不含 BOM），导致响应体与声明长度不匹配，连接被提前关闭，客户端只收到 BOM 3 字节。

**根因**：`res.write()` + `res.send()` 的组合在 Express 4 中行为不一致，Content-Length header 未包含 BOM 的长度。

**修复**（2 处）：
- `api/routes/audit.ts`：两处导出路由都从 `res.write(BOM) + res.send(csvContent)` 改为 `res.end(BOM + csvContent)` 一次性输出

**验收标准**：导出文件 > 100 字节，内容包含批次/样本/温控/交接链四张表，筛选条件在文件头注释中。

---

## 🛠 开发命令

| 命令 | 说明 |
|---|---|
| `npm run dev` | 同时启动前后端（推荐） |
| `npm run server:dev` | 仅后端（3001） |
| `npm run client:dev` | 仅前端（5173） |
| `npm run build` | 生产构建 |
| `npm run lint` | ESLint 检查 |
| `npm run check` | TypeScript 类型检查 |
